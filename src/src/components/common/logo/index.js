import React from 'react'

const index = ({className}) => {
  return (
    <div class= {className + 
    " font-extrabold background-animate select-none mx-0 w-auto text-transparent text-xl bg-clip-text bg-gradient-to-r from-indigo-500 via-purple-500 to-pink-500 ..."}>ErasmusNET</div>
  )
}

export default index